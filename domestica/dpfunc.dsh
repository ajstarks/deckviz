def dp DataX DataY DataSize DataLabel DataFile Ropt

	dl=DataX - sparkw2
	dr=dl + sparkw
	db=DataY + DataSize
	dt=db + sparkh
	ly=DataY-1
	dchart sparkopt Ropt -top dt -bottom db -left dl -right dr DataFile
	ctext DataLabel DataX ly ls "serif"

edef